import { MaxMinAvgResponse } from './max-min-avg-response';

describe('MaxMinAvgResponse', () => {
  it('should create an instance', () => {
    expect(new MaxMinAvgResponse()).toBeTruthy();
  });
});
